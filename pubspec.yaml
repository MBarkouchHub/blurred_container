name: blurred_container
description: A Flutter widget for creating blurred, frosted-glass styled containers with a simple API.
version: 0.0.1
homepage: https://marruecode.com
repository: https://github.com/MBarkouchHub/blurred_container
issue_tracker: https://github.com/MBarkouchHub/blurred_container/issues
license: MIT

environment:
  sdk: ^3.9.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter: