## 0.0.1
- First release of `BlurredContainer`
- Supports all standard `Container` properties plus a customizable `blur` value
- Fixed a bug where providing both `color` and `decoration.color` caused an error.
- The widget now safely merges the `color` property when a `BoxDecoration` is used.

